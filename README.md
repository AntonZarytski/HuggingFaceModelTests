# 🤖 Multi-Model AI Chat

Веб-приложение для взаимодействия с несколькими моделями искусственного интеллекта через HuggingFace Inference API.

## 📋 Описание

Это приложение позволяет общаться с различными AI-моделями в удобном веб-интерфейсе, сравнивать их ответы, отслеживать статистику использования и стоимость запросов.

## 🎯 Основные возможности

- **Множественные модели**: Поддержка 3 различных AI-моделей с разными характеристиками
- **Единообразный интерфейс**: Одинаковый system prompt для всех моделей
- **Мониторинг ресурсов**: Отслеживание токенов, времени ответа и стоимости
- **Гибкие настройки**: Настройка максимального количества токенов в ответе
- **Современный UI**: Адаптивный дизайн с темной темой

## 🤖 Поддерживаемые модели

### 1. **DeepSeek V3.2** (685B параметров)
- **Провайдер**: Novita
- **Тип**: Платная модель
- **Стоимость**: $0.28/1M входных токенов, $0.42/1M выходных токенов
- **Max tokens**: 2000
- **Описание**: Мощная модель для сложных задач
- **Ссылка**: https://huggingface.co/deepseek-ai/DeepSeek-V3.2

### 2. **SmolLM3-3B** (3B параметров)
- **Провайдер**: HuggingFace Inference
- **Тип**: Бесплатная модель
- **Context window**: 8192 токенов
- **Max tokens**: 2000
- **Описание**: Эффективная модель среднего размера от Hugging Face
- **Ссылка**: https://huggingface.co/HuggingFaceTB/SmolLM3-3B

### 3. **Qwen 2.5 0.5B Instruct** (0.5B параметров)
- **Провайдер**: Featherless AI
- **Тип**: Платная модель
- **Стоимость**: $0.01/1M входных токенов, $0.015/1M выходных токенов
- **Max tokens**: 2000
- **Описание**: Компактная и быстрая модель для простых задач
- **Ссылка**: https://huggingface.co/adrimoreau/Qwen2.5-0.5B-Instruct-Gensyn-Swarm-untamed_insectivorous_coyote

## 🛠️ Технологический стек

### Backend
- **Python 3.x**
- **Flask** - веб-фреймворк
- **huggingface_hub** - библиотека для работы с HuggingFace API
- **python-dotenv** - управление переменными окружения
- **tiktoken** (опционально) - точный подсчет токенов

### Frontend
- **HTML5/CSS3** - структура и стилизация
- **Vanilla JavaScript** - логика интерфейса
- **Font Awesome** - иконки
- **Google Fonts (Inter)** - типографика

## 📦 Установка

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd multi-model-ai-chat
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения
Создайте файл `.env` в корне проекта:
```env
HUGGINGFACE_API_TOKEN=your_huggingface_token_here
PORT=8000
```

### 4. Запуск приложения
```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:8000`

## 📁 Структура проекта

```
.
├── app.py              # Основной Flask-сервер
├── public/
│   └── index.html      # Веб-интерфейс
├── .env                # Переменные окружения (не в репозитории)
├── requirements.txt    # Python зависимости
└── README.md          # Документация
```

## 🔧 Конфигурация

### Настройка моделей
Модели настраиваются в файле `app.py` в словаре `MODELS`. Каждая модель имеет следующие параметры:

```python
"model_key": {
    "id": "huggingface_model_id",
    "provider": "provider_name",
    "task": "chat_completion",
    "api_method": "chat_completions_create",
    "is_free": True/False,
    "max_tokens": 2000,
    "input_price_per_mt": 0.01,    # для платных моделей
    "output_price_per_mt": 0.015,  # для платных моделей
    "model_size": "3B params",
    "description": "Описание модели"
}
```

### System Prompt
Единый system prompt для всех моделей задается в константе `SYSTEM_PROMPT`:

```python
SYSTEM_PROMPT = """You are a helpful AI assistant. 
You provide clear, accurate, and concise answers.
Always respond in the same language as the user's question."""
```

## 📊 API Endpoints

### `GET /api/models`
Получить список доступных моделей с их характеристиками.

### `POST /api/generate`
Генерация ответа от выбранной модели.

**Тело запроса:**
```json
{
  "model_key": "deepseek",
  "prompt": "What is the capital of France?",
  "max_tokens": 1200
}
```

**Ответ:**
```json
{
  "model_key": "deepseek",
  "response_text": "The capital of France is Paris.",
  "time_s": 2.45,
  "input_tokens": 8,
  "output_tokens": 12,
  "total_tokens": 20,
  "estimated_cost_usd": 0.00000728
}
```

### `GET /api/model_stats`
Статистика по всем моделям.

## 💡 Особенности реализации

### Обработка ответов
- Универсальный метод извлечения ответов из разных форматов API
- Специальная обработка для SmolLM3 модели
- Очистка ответов от артефактов форматирования

### Подсчет токенов
- Использование `tiktoken` для точного подсчета (если установлен)
- Fallback на приблизительный подсчет (слова × 1.3)

### Расчет стоимости
- Автоматический расчет стоимости для платных моделей
- Отображение `$0.00` для бесплатных моделей
- Точность до 8 знаков после запятой

## 📈 Статистика использования

Приложение отслеживает:
- ✅ Общее количество запросов
- ✅ Суммарное количество использованных токенов
- ✅ Общую стоимость запросов
- ✅ Среднее время ответа моделей

**Примечание**: Для использования приложения необходим валидный HuggingFace API токен. Получить токен можно на [huggingface.co](https://huggingface.co/settings/tokens).
